{
  "name": "ops-engine-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply OPS_ENGINE_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply OPS_ENGINE_DB --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260401.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.13.0"
  }
}
